# Webordo SRM — зависимости (Python 3.12–3.14)
# Установка:  pip install -r requirements.txt

# --- Framework ---
# 5.2 — LTS-ветка, официально поддерживает Python 3.14 (5.1 — нет, падает на admin
# из-за copy(super()) в template/context.py на 3.14). Апгрейд проверен: check + все
# тесты проходят без предупреждений.
Django==5.2.17
asgiref==3.12.1
sqlparse==0.6.0

# --- PostgreSQL (драйвер psycopg 3 + бинарная сборка) ---
psycopg==3.3.5
psycopg-binary==3.3.5

# --- Конфигурация через .env ---
python-dotenv==1.2.3

# --- Импорт/экспорт Excel (.xlsx) ---
openpyxl==3.1.5
et-xmlfile==2.0.0
