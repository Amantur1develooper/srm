from django.apps import AppConfig


class FinsovetConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.finsovet"
    label = "finsovet"
    verbose_name = "Финсовет"
